extends ../layout

block content
  h1 Login
  .container
    form(action="/login",method="post")        
            .form-group
                label(for="email") Email
                input#email(type="text", class="form-control", required name="email" value="admin@gmail.com")
            .form-group
                label(for="password") Password
                input#price(type="password", class="form-control",required name="password", value="admin")
            input(type="submit", class="btn btn-info" value="Login")
           